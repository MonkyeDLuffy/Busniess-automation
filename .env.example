# Copy to .env and fill in values.

# ── Server ──
PORT=5050

# Required — generates signed login cookies. Make it long & random.
AUTH_SECRET=

# ── Redis / Vercel KV (required when deployed) ──
# On Vercel install Upstash Redis (Vercel Marketplace) and link it; the env vars
# are injected automatically. Locally you can use Upstash console values.
KV_REST_API_URL=
KV_REST_API_TOKEN=
# (or) UPSTASH_REDIS_REST_URL=
# (or) UPSTASH_REDIS_REST_TOKEN=

# ── Scraping performance ──
CONCURRENCY_LIMIT=5

# ── Google Sheets (optional) ──
# Either a path to the service-account JSON, or the full JSON as a single line.
GOOGLE_SERVICE_ACCOUNT_KEY=
SPREADSHEET_ID=
SHEET_TAB=Businesses

# ── NocoDB (primary destination for found businesses) ──
NOCODB_URL=https://app.nocodb.com
NOCODB_TOKEN=
NOCODB_BASE_ID=
NOCODB_TABLE_ID=
NOCODB_TABLE_NAME=Business-automation

# ── Resend email (optional) ──
RESEND_API_KEY=
RESEND_FROM=
UNSUBSCRIBE_URL=

# ── Email lookup fallback (optional) ──
HUNTER_API_KEY=
